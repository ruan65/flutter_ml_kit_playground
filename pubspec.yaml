name: firebase_mlkit_playground
description: ML Flutter playground project

version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  camera:
  firebase_core: ^0.4.4+3
  firebase_ml_vision: ^0.9.3+8
  firebase_analytics: ^5.0.11
  cupertino_icons: ^0.1.3
  image_picker: ^0.6.4
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true